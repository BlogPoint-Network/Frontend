import React from 'react';

const MyChannels = () => {
    return (
        <div>
            <h1>Подписки (список каналов на которые подписан)</h1>
        </div>
    );
};

export default MyChannels;